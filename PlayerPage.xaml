<Page
    x:Class="levyke.PlayerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:levyke"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <MediaPlayerElement x:Name="mediaPlayer" AutoPlay="False"/>

        <Image x:Name="FullAlbumArt"
               Width="140"
               Height="140"
               HorizontalAlignment="Left"
               VerticalAlignment="Bottom"
               Margin="20,0,0,120"/>

        <Button x:Name="BackButton"
                Content="←"
                FontSize="24"
                Width="50"
                Height="50"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Margin="16,16,0,0"
                Background="Transparent"
                Foreground="White"
                Click="BackButton_Click"/>

        <TextBlock x:Name="FullTrackTitle"
                   Foreground="White"
                   FontSize="24"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Bottom"
                   Margin="170,0,0,200"
                   MaxWidth="300"
                   TextWrapping="Wrap"/>

        <TextBlock x:Name="FullTrackArtist"
                   Foreground="Gray"
                   FontSize="18"
                   HorizontalAlignment="Left"
                   VerticalAlignment="Bottom"
                   Margin="170,0,0,180"
                   MaxWidth="300"
                   TextWrapping="Wrap"/>

        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Bottom"
                    Margin="15,0,0,60">
            <TextBlock x:Name="CurrentTimeText" Text="0:00" Foreground="White" Margin="0,0,10,0"/>
            <Slider x:Name="ProgressSlider"
                    Width="250"
                    Height="30"
                    Foreground="White"
                    ValueChanged="ProgressSlider_ValueChanged"/>
            <TextBlock x:Name="TotalTimeText" Text="0:00" Foreground="White" Margin="10,0,0,0"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Bottom"
                    Margin="10,0,0,10">
            <Button x:Name="PrevButton" Content="⏮" Click="PrevButton_Click" 
                    Background="Transparent" Foreground="White" FontSize="24" Margin="0,0,10,0"/>
            <Button x:Name="PlayPauseButton" Content="⏸" Click="PlayPauseButton_Click" 
                    Background="Transparent" Foreground="White" FontSize="24"/>
            <Button x:Name="NextButton" Content="⏭" Click="NextButton_Click" 
                    Background="Transparent" Foreground="White" FontSize="24"/>
            <Button x:Name="RepeatButton" Content="⊞" Click="RepeatButton_Click"
                    Background="Transparent" Foreground="White" FontSize="24" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</Page>
