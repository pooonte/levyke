<Page
    x:Class="levyke.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:levyke"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{StaticResource MainBackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="9*"/>
            <RowDefinition Height="55*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="levyke" Foreground="{StaticResource AppTitleBrush}" Margin="16,16,0,0" FontSize="24" HorizontalAlignment="Left" VerticalAlignment="Top"/>
        <Pivot Margin="0,53,0,0" Grid.RowSpan="2">
            <PivotItem Header="Медиатека" FontSize="22">
                <ListView x:Name="TracksList" ItemClick="Track_ItemClick" IsItemClickEnabled="True">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding DisplayName}" Foreground="White"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </PivotItem>
 <!--           <PivotItem Header="Исполнители" FontSize="22">
                <ListView x:Name="NameList" ItemClick="Names_ItemClick" IsItemClickEnabled="True">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding DisplayName}" Foreground="White"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </PivotItem>
            <PivotItem Header="Альбомы" FontSize="22">
                <ListView x:Name="AlbumList" ItemClick="Albums_ItemClick" IsItemClickEnabled="True">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding DisplayName}" Foreground="White"/>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </PivotItem> -->
            <PivotItem Header="Тема">
                <StackPanel Padding="20" Margin="16">
                    <TextBlock Text="Выберите тему"
                           Foreground="White" />

                    <ComboBox x:Name="ThemeSelector"
                          SelectionChanged="ThemeSelector_SelectionChanged"
                          Foreground="{StaticResource AppTitleBrush}"
                          Background="{StaticResource MiniPlayerBackgroundBrush}" />
                </StackPanel>
            </PivotItem>
        </Pivot>
        <Grid x:Name="MiniPlayerPanel"
          VerticalAlignment="Bottom"
          Background="{StaticResource MiniPlayerBackgroundBrush}"
          Height="60"
          Visibility="Collapsed" Grid.Row="1">

            <Button x:Name="OpenPlayerButton"
                HorizontalAlignment="Left"
                VerticalAlignment="Stretch"
                Background="Transparent"
                BorderThickness="0"
                Width="250"
                Click="OpenPlayerButton_Click">
                <StackPanel Orientation="Horizontal" Margin="12,0">
                    <Image x:Name="MiniAlbumArt" Width="40" Height="40" Stretch="UniformToFill"/>
                    <StackPanel Margin="10,0,0,0" VerticalAlignment="Stretch" HorizontalAlignment="Left">
                        <TextBlock x:Name="MiniTrackTitle" Foreground="White" FontSize="14" MaxWidth="150" TextTrimming="CharacterEllipsis"/>
                        <TextBlock x:Name="MiniTrackArtist" Foreground="Gray" FontSize="12" MaxWidth="150" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </StackPanel>
            </Button>

            <Button x:Name="MiniPlayButton"
                Content="▶"
                FontSize="18"
                Width="40"
                Height="40"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                Margin="0,0,16,0"
                Background="{StaticResource PlaybackControlBrush}"
                Foreground="{StaticResource PlaybackControlForegroundBrush}"
                Click="MiniPlayButton_Click"/>
        </Grid>
    </Grid>
</Page>
